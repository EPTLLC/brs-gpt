# BRS-GPT Configuration Example
# Copy this file to .env and fill in your actual API key
# 
# Usage:
#   cp .env.example .env
#   nano .env  # Edit with your real API key

# Required: OpenAI API Key
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: AI Models (ultra-cheap by default)
# Primary analysis model (default: gpt-5-mini - $0.25/$2.00 per 1M tokens)
OPENAI_MODEL=gpt-5-mini

# Search/classification model (default: gpt-4o-mini - $0.15/$0.60 per 1M tokens)
OPENAI_SEARCH_MODEL=gpt-4o-mini-search-preview

# Fallback model for strict JSON (default: gpt-5-nano - $0.05/$0.40 per 1M tokens)
OPENAI_FALLBACK_MODEL=gpt-5-nano

# Optional: API Server Settings (for REST API mode)
# API_KEY=your-secure-api-key-here

# NOTE: Costs are negligible ($0.02-$0.15 per scan)
